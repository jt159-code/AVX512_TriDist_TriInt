scp -r D:\VS\final\VScode_PQP_Tridist_avx3 juting@101.76.222.120:~/my_project
ssh juting@101.76.222.120
rm -rf ~/my_project/*
 g++ -mavx512f -O2 -o myprogram main.cpp TriDist.cpp TriInt.cpp
g++ -o myprogram main.cpp TriDist.cpp TriInt.cpp -mavx512f

cd "d:\VS\final"
git status
git remote add origin https://github.com/jt159-code/AVX512_TriDist_TriInt.git
# 什么都不用做，你已经在 master 分支了
# 或者确认一下当前分支
git branch
# 切换分支
git checkout master
# 或者更现代的写法
git switch master
# 1. 查看当前状态
git status

# 2. 恢复 .gitignore
git restore .gitignore

# 3. 添加所有文件（.gitignore会排除临时文件）
git add .

# 4. 提交
git commit -m "Update project: all algorithm versions"

# 5. 推送到 GitHub
git push -u origin master

# 1. 查看当前状态
git status

# 2. 添加修改的文件
git add .
# 或指定文件
git add 文件名.cpp

# 3. 提交修改
git commit -m "描述你的修改"

# 4. 推送到远程master（因为已经关联过，只需 git push）
git push

PQP_Tridist———基础的，可在VS上运行的版本
AVX512——AI修改了基础运算的，可在VS上运行
VScode_PQP_Tridist_avx0——版本0，可在VScode上运行的，未进行AVX512准备的原始版本
VScode_PQP_Tridist_avx1——版本1，VScode，进行了结构体修改分支化，数据驱动setpoint，未AVX512向量化
VScode_PQP_Tridist_avx2——版本2，VScode，1对三角形的算法改为8对/16对，进行AoS->SoA，为AVX512做准备
VScode_PQP_Tridist_avx3——版本3，VScode，进行一次AVX５１２向量化对比
VScode_PQP_Tridist_avx4——版本4，Linux，无加速
VScode_PQP_Tridist_avx5——版本5，Linux，加速