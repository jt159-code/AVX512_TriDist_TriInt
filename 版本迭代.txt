scp -r D:\VS\final\AVX512_TriDist_TriInt_3 juting@101.76.222.120:~/my_project
ssh juting@101.76.222.120
rm -rf ~/my_project/*
 g++ -mavx512f -O2 -o myprogram main.cpp TriDist.cpp TriInt.cpp
g++ -o myprogram main.cpp TriDist.cpp TriInt.cpp -mavx512f

cd "d:\VS\final"
git status
git remote add origin https://github.com/jt159-code/AVX512_TriDist_TriInt.git
##### 什么都不用做，你已经在 master 分支了
##### 或者确认一下当前分支
git branch
##### 切换分支
git checkout master
##### 或者更现代的写法
git switch master
##### 1. 查看当前状态
git status

##### 2. 恢复 .gitignore
git restore .gitignore

##### 3. 添加所有文件（.gitignore会排除临时文件）
git add .

##### 4. 提交
git commit -m "Update project: all algorithm versions"

##### 5. 推送到 GitHub
git push -u origin master

##### 1. 查看当前状态
git status

##### 2. 添加修改的文件
git add .
##### 或指定文件
git add 文件名.cpp

##### 3. 提交修改
git commit -m "描述你的修改"

##### 4. 推送到远程master（因为已经关联过，只需 git push）
git push

##### master分支（最新版本avx5）
git checkout -b master
git add .
git commit -m "Initial commit: all versions"

##### 创建版本分支
git checkout -b version-avx0
git add AVX512_TriDist_TriInt_0/
git commit -m "Version 0: 原始版本"

git checkout -b version-avx1
git add AVX512_TriDist_TriInt_1/
git commit -m "Version 1: 结构体修改分支化"

git checkout -b version-avx2
git add AVX512_TriDist_TriInt_2/
git commit -m "Version 2: 8对/16对算法"

git checkout -b version-avx3
git add AVX512_TriDist_TriInt_3/
git commit -m "Version 3: AVX512向量化对比"

git checkout -b version-avx4
git add AVX512_TriDist_TriInt_4/
git commit -m "Version 4: Linux无加速"

git checkout -b version-avx5
git add AVX512_TriDist_TriInt_5/
git commit -m "Version 5: Linux加速"

##### 回到master分支
git checkout master


## 怎么完全清空master分支
# 1. 切换到master分支
git checkout master

# 2. 删除所有文件（保留.git目录）
git rm -rf .

# 3. 提交清空操作
git commit -m "Remove all files"

# 4. 强制推送到远程
git push -f origin master

PQP_Tridist———基础的，可在VS上运行的版本
AVX512——AI修改了基础运算的，可在VS上运行
AVX512_TriDist_TriInt_0——版本0，可在VScode上运行的，未进行AVX512准备的原始版本
AVX512_TriDist_TriInt_1——版本1，VScode，进行了结构体修改分支化，数据驱动setpoint，未AVX512向量化
AVX512_TriDist_TriInt_2——版本2，VScode，1对三角形的算法改为8对/16对，进行AoS->SoA，为AVX512做准备
AVX512_TriDist_TriInt_3——版本3，VScode，进行一次AVX５１２向量化对比
AVX512_TriDist_TriInt_4——版本4，Linux，无加速
AVX512_TriDist_TriInt_5——版本5，Linux，加速